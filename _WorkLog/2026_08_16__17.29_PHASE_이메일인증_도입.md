# 이메일 인증(Confirm email) 도입

## 배경
- Supabase Auth `mailer_autoconfirm: true` — 2026-02-02 프로젝트 셋업 때부터 의도적으로 OFF
  (근거: `supabase/schema.sql:200` "설정 완료 후 할 일: 1. Confirm email OFF")
- 현재 가입 코드는 "signUp 직후 세션이 즉시 발급된다"는 전제로 작성됨
- 설정만 켜면 가입이 깨지므로 **코드 수정 선행 → 검증 → 설정 변경** 순서

## Phase 체크리스트

- [x] Phase 1: `Auth.signUp()`에 `emailRedirectTo` 추가 + 전체 프로필 필드를 metadata로 전달
- [x] Phase 2: 가입 핸들러 세션 유무 분기 (session null → "메일함 확인" 안내로 종료)
- [x] Phase 3: 인증 후 첫 로그인 시 프로필 완성 루틴 (`completePendingProfile`) 구현 + SIGNED_IN/initAuth 연결
- [x] Phase 4: 근본버그 수정 — interests(TEXT[]) 정규화 / db.js 로드실패 / about_me / 예비멤버 병합 RPC
- [x] Phase 5: 배포 (git push → Vercel) — 커밋 `3579c1f`, `301c6c0`(캐시버스팅), `2299df4`(config)
- [x] Phase 6: Supabase Confirm email ON — `mailer_autoconfirm: false` 확인
- [x] Phase 7: E2E — 메일 링크 클릭 → 로그인 → 예비멤버 병합·프로필 완성 확인 **(2026-08-16 18:5x 실측 통과, 아래 §E2E 최종 결과)**

## 후속 — 예비멤버 전환 유도 안내문 (PO 지시 2026-08-16 19:0x)

PO 방침: **예비멤버 92명에게 일괄 안내 발송은 하지 않는다.** 모임 신청 등 자연스러운 접점에서 전환을 유도한다.
→ 그러려면 가입 모달이 예비멤버를 알아보고 안내해야 하는데, 현재 그 안내가 **화면에 뜨지 않는다**(문제 A).

- [x] Phase 8: `index.html` 에 `js/db.js` 로드 추가 (현재 미로드 → `DB.getProfileByEmail()` 부재)
- [x] Phase 9: 이메일 입력 시 예비멤버 감지 → 안내 배너 표시 + 전환 신청 유도
      문구(PO 지정): "예비 멤버로 등록되어 있습니다. 정규 멤버로 전환 신청을 하여 주세요."
- [x] Phase 10: 배포 — 커밋 `2dd7589`(기능) + `10938fe`(IIFE 수정), 라이브 반영 확인
- [x] Phase 12: 인증 메일 한글화 — 제목 `[WAAT] 이메일 인증을 완료해 주세요`, 본문 WAAT 브랜드 템플릿
- [x] Phase 13: 커스텀 SMTP(Resend) 연결 — 발신자·발송상한 문제 해결
- [x] Phase 11: `w2center@naver.com` 로 가입~인증~전환 E2E 재검증 **(2026-08-16 20:06 통과 — 아래 §최종 E2E)**

## ✅ 최종 E2E — 예비멤버 → 정규멤버 전환 전 구간 통과 (2026-08-16 20:06)

PO 가 실제 브라우저·실제 네이버 메일함으로 전 구간 수행. DB 실측 결과:

| 검증 | 결과 |
|---|---|
| auth 계정 생성 | `16f6ce04-f79f-4406-83bf-8d6a7ec77aea`, 11:05:50 |
| 이메일 인증 | `email_confirmed_at = 11:06:36` ✅ |
| 인증 후 로그인 | `last_sign_in_at = 11:06:36` ✅ |
| profiles 행 수 | **1건** (중복 없음) ✅ |
| 옛 예비멤버 행 `27ee097a…` | **삭제됨** — `claim_provisional_profile()` 이 흡수 ✅ |
| 예비멤버 마크 | `notes = null` — 제거됨 ✅ |
| 프로필 복원 | `name=승승센터`, `phone=01050678306`, `message=안녕` (선택 입력까지 보존) ✅ |
| interests | `[]` (미입력, TEXT[] 정규화 정상) ✅ |

→ **예비멤버 92명은 이제 모임 신청 접점에서 안내를 보고 스스로 정규 전환할 수 있다.** 일괄 안내 발송 불필요(PO 방침).

## 후속 정리 (2026-08-16 21:xx ~ 22:xx) — PO 확인 완료

### 고아 페이지 삭제
`join.html` + `js/join.js` 삭제 (커밋 `ad9f16d`). 어디서도 링크되지 않던 페이지로, 현행 예비멤버 체계
(`profiles.notes` 마크)가 아닌 별도 `provisional_members` 테이블을 쓰고 있었다.
테이블(111건)과 `db.js` 의 관련 함수는 보존 — 데이터가 살아있고 admin 에서 쓸 수 있다.
라이브 404 확인 (`/join.html` → 308 → `/join` → 404, `js/join.js` → 404).

**두 명단 대조 결과**: `provisional_members` 111명 **전원**이 `profiles` 에 이미 정리돼 있었다
(87명 예비 마크 / 24명은 이미 정규회원). 누락 0명 — 별도 조치 불필요.
수집 원본 엑셀 2개(`111명_예비멤버_등록용.xlsx`, `전체_연락처_최종.xlsx`)는 PO 지시로 삭제.

### 관리자 페이지 버그 3건 수정 (커밋 `3834982`·`f02254e`·`96fb59d`·`a394feb`)

| # | 문제 | 원인 | 수정 |
|---|---|---|---|
| ① | 관리자 페이지를 열면 멤버 목록이 **빈 화면** | `#panel-members` 에 `active` 누락 — 탭 버튼만 active, 패널은 `display:none`. 탭을 한 번 눌러야 나왔다 | `class="admin-panel active"` |
| ② | 예비 멤버 표시가 화면에 **전혀 안 보임**(0건) | 비고(`notes`) 컬럼 자체가 없었다. 데이터는 `getAllProfiles()` 가 `select('*')` 로 이미 받아오고 있었다 | 비고 컬럼 + 뱃지 + 행 강조, 구분 필터(전체/예비/정규), 카운트 `전체 N · 정규 N · 예비 N`, 검색에 이메일 추가, 내보내기에 구분·비고 포함 |
| ③ | Excel·CSV 다운로드 **둘 다 먹통** | SheetJS 를 CSP 미허용 `cdn.sheetjs.com` 에서 로드 → 차단. CSV 도 XLSX 를 쓰므로 같이 죽었다 | 허용된 jsdelivr 로 교체. ⚠️ `xlsx@0.20.3` 은 jsdelivr 에 없어(SheetJS npm 배포 중단) **0.18.5 로 고정** |

**함정 기록**: `admin.html` 인라인 `<style>` 이 컬럼 폭을 `nth-child` **순서 번호**로 지정한다.
비고를 7번에 넣자 가입일·관리가 한 칸씩 밀려 잘렸다. 컬럼을 추가/삭제하면 그 번호도 반드시 함께 고칠 것(경고 주석 추가함).

## Google OAuth 병합 누락 (2026-08-17) — 수정 완료

**발단**: PO 가 "오늘 2명 전환됐다는 건 어디서 나온 데이터냐" 고 물었다. 실은 카운트 차이(92→90)로
추론한 것이었고 근거가 약했다. 제대로 조회하다 진짜 버그를 찾았다.

**증상**: 8/16 수집명단 가입자 11명 중 6명이 병합 실패 — `profiles` 행이 2개로 남고 예비 마크 잔존.
그중 5명이 **Google 로그인**, 1명(`sysmining`)은 병합 기능 배포 전 가입.

**원인** (`js/main.js`): `completePendingProfile()` 은 첫 줄에서
`if (!meta.waat_profile_pending) return;` 로 빠져나가는데, 예비멤버 병합 RPC 호출이 그 아래 있었다.
`waat_profile_pending` 은 **우리 가입 폼으로 가입할 때만** 심기는 표시다.
→ Google 로 들어온 사람은 병합이 아예 실행되지 않았다.

**수정**: 병합을 `claimProvisionalIfAny()` 로 분리해 early return **위로** 올렸다 (커밋 `c970ee1`).
- 모든 로그인 경로에서 페이지당 1회 실행, 실패해도 로그인은 진행
- Google OAuth 는 `redirectTo = origin + '/'` 로 항상 index 로 돌아오고, `main.js` 는 index 에만 실려 있다 → 반드시 통과
- 한 번 실패해도 다음 방문 때 재시도되는 자가 치유 구조

**검증**: `waat_profile_pending` 없는 계정(= Google 과 동일 조건) + 예비행 2건 조건을 만들어 실로그인.
2행 → 1행, 예비 마크 제거, 출처 승계, 콘솔 `예비멤버 행 병합 완료` 확인.

**부수 수정**: 승계 notes 에 구분자가 남던 문제(`· 카톡_댓글`) — RPC 의 `btrim` 에 가운뎃점을 포함시켜
`카톡_댓글` 로 정리 (마이그레이션 `20260817010000`, 커밋 `486dbe5`).

**데이터 정리**: 어긋난 6명의 고아행을 병합·삭제. 실명 행을 살리고 출처는 비고에 승계.
삭제분 백업 → `_WorkLog/2026_08_17__삭제백업_예비멤버_고아행_6건.json`
정리 후: 예비 **84명**, 이메일 중복행 0, 가입했는데 예비로 남은 사람 0명.

### 🔸 남은 한계 — 이메일 불일치 (PO 판단: **그냥 둔다**, 2026-08-17)

예비명단 주소와 Google 계정 주소가 다르면(예: 카톡엔 naver, 로그인은 gmail) 시스템은 두 사람을
**연결할 근거가 없다**. 이름은 카톡 닉네임 ↔ 실명이라 무의미하고, 예비행엔 전화번호가 없다.
→ 자동 감지 불가. 관리자 감지 장치를 붙여도 이 경우는 못 잡는다.

규모: 남은 84명 중 gmail 42명(매칭됨) / naver·한메일 등 **42명**(Google 로그인 시 못 잡음).
단, 이메일·비밀번호로 가입하면 주소를 직접 입력하므로 정상 매칭된다.

**영향**: 회원 본인에게는 아무 문제 없다(정상 가입·로그인·모임 신청, 전환 안내도 안 뜬다).
피해는 예비 명단에 유령 한 줄이 남아 **숫자가 실제보다 부풀어 보이는 것**뿐이다.

**PO 결정**: 감지 장치·가입 흐름 추가 질문 모두 하지 않는다. 예비 명단을 근사치로 받아들이고,
시간이 지나도 안 줄어드는 이름은 PO 가 보고 수동 판단한다.
→ **다음 세션에서 이 건으로 다시 제안하지 말 것.**

### ✅ 실브라우저 검증 (Playwright, 배포본)
첫 화면 목록 표시 / 뱃지 90개 / 예비 필터 90행 전부 예비 / 정규 필터 189행 예비 미혼입 /
필터+검색 동시 적용 / Excel·CSV 실제 다운로드 성공 / 콘솔 에러 0건. **PO 육안 확인 완료.**

## 인증 메일 한글화 + Resend SMTP (2026-08-16 19:5x)

### 발단 — "인증메일이 오지 않음"
원인은 **이메일 오타**였다. PO가 모바일에서 `w2cente**t**@naver.com`(끝 t)으로 입력.
그 주소로 계정·인증메일 모두 정상 생성·발송됐고, 존재하지 않는 주소라 도달하지 않았을 뿐이다.
→ 발송 시스템 자체는 정상이었음. (오타 계정 `c4505e89…` 는 PO 승인 대기 중 미삭제)

### 조치 1 — 메일 한글화
| 항목 | 값 |
|---|---|
| 제목 | `[WAAT] 이메일 인증을 완료해 주세요` |
| 본문 | WAAT 헤더 + 인증 버튼 + 링크 복사용 주소 + 만료 안내, 원본 `supabase/templates/confirmation-ko.html` |
| 적용 경로 | **Management API PATCH** — `config push` 는 config.toml 에 없는 설정을 CLI 기본값으로 덮어쓰므로 사용하지 않음 |
| 부작용 점검 | site_url·redirect 5개·Google OAuth·MFA·autoconfirm 전부 불변 확인 |

### 조치 2 — 커스텀 SMTP(Resend) 연결 ⭐ 실제 병목이었음
`rate_limit_email_sent` 가 **2** 였다 — 기본 Supabase SMTP 는 **시간당 2통**.
예비멤버 92명이 몰려 가입하면 세 번째부터 인증 메일이 아예 나가지 않는 구조였다.

| 항목 | 이전 | 이후 |
|---|---|---|
| SMTP | (Supabase 기본) | `smtp.resend.com:465`, user `resend` |
| 발신자 | `Supabase Auth <noreply@mail.app.supabase.io>` | `WAAT <noreply@waat.community>` |
| 시간당 발송 상한 | **2** | **30** (Resend 무료 일 100통 안) |

`waat.community` 는 Resend 에서 이미 `verified` (2026-05-19 등록, AI Biz Daily 발송에 사용 중).

### ✅ 실발송 검증 (설정 확인이 아니라 실제 메일)
`wksun999+waatsmtp@gmail.com` 로 실제 signUp → Resend 로그 조회:
```
보낸사람 : "WAAT" <noreply@waat.community>
받는사람 : wksun999+waatsmtp@gmail.com
제목     : [WAAT] 이메일 인증을 완료해 주세요
상태     : delivered ✅
```
검증용 테스트 계정(auth + profiles)은 삭제 완료. `w2center` 예비멤버 행은 그대로 유지 확인.

### 조치 3 — 가입폼 문구 줄바꿈 (PO 지적)
"현재 하는 일·관심분야·하고 싶은 말 (선택, 입력하려면 클릭)" 이 모바일에서 어색하게 잘려
동의 체크박스와 붙어 보였다 → 명시적 2줄 + 상하 여백. iPhone 390px 실렌더 검증(가로 넘침 없음).

### 🚨 배포 후 발견·수정 — `db.js` 가 index.html 에서 통째로 죽었다

Phase 8 로 `db.js` 를 넣자마자 **`SyntaxError: Identifier 'supabase' has already been declared`** 로 파일 전체가 미로드됐다.
원인: `db.js:12` 의 전역 `const supabase = _supabase;` 가 CDN 이 선언한 전역 `supabase` 와 충돌.
(admin.html·join.html 에서는 우연히 안 터졌지만 index.html 은 CDN 로드 순서가 달라 터졌다)

**수정**: 파일 전체를 IIFE `(function(){ ... })()` 로 감싸 별칭을 지역 변수로 격리 (커밋 `10938fe`).
**교훈**: 이 프로젝트의 전역 스크립트는 `const`/`let` 최상위 선언이 지뢰다. `var` 도 재선언은 되지만 값 덮어쓰기 사고가 나므로, 새 전역 스크립트는 IIFE 로 감싼다.

### ✅ 실브라우저 클릭 검증 (Playwright, headless, 배포본 대상)

| 검증 | 결과 |
|---|---|
| `DB.getProfileByEmail` 로드 | ✅ 존재 |
| 비로그인 상태로 모임 시간대 버튼 클릭 → 가입 모달 | ✅ 열림 |
| 비로그인 안내 문구 | ✅ "모임 참석 신청을 하려면 멤버 가입을 해주세요." |
| 예비멤버 이메일 입력 → 안내 표시 | ✅ "승승센터님, **예비 멤버로 등록되어 있습니다.** 정규 멤버로 전환 신청을 하여 주세요." |
| 이름 자동 채움 | ✅ `승승센터` |
| 대조군(미등록 이메일) 오탐 | ✅ 안내 뜨지 않음 |
| 콘솔 에러 | ✅ 없음 |

검증 스크립트·스크린샷: 세션 scratchpad `test-provisional.js`, `2-예비멤버안내.png`

## 🚨 사고 기록 — config push 로 운영 auth 설정 초기화 (2026-08-16 08:5x, 복구 완료)

**무슨 일**: 최소 `config.toml`(project_id + `[auth] enable_confirmations`)로 `supabase config push` 실행.
`--yes` 없이도 **확인 프롬프트 없이 즉시 적용**됐고, config.toml 에 없는 항목이 CLI 기본값으로 덮어써졌다.

| 항목 | 원래값 | 덮어써진 값 |
|---|---|---|
| `site_url` | `https://waat.community` | `http://127.0.0.1:3000` |
| `additional_redirect_urls` | 운영 5개 | `["https://127.0.0.1:3000"]` |
| `mfa.totp.enroll/verify` | true | false |
| `email.max_frequency` | 1m0s | 1s |
| `email.otp_length` | 8 | 6 |

게다가 `enable_confirmations` 를 `[auth]` 아래 뒀는데 실제 키는 `[auth.email]` 이라 **목표도 달성 못 했다.**

**영향**: 이 상태에서 Google OAuth 로그인·비밀번호 재설정 링크가 127.0.0.1 로 향했을 것. 노출 시간 약 1~2분.

**복구**: 위 diff 의 remote 측 값을 그대로 `config.toml` 에 명시하고 재푸시 → 전부 원복 + `[auth.email] enable_confirmations = true` 적용.

**재발 방지**: `config.toml` 을 저장소에 커밋(`2299df4`)해 운영값을 고정. 파일 상단에 경고 주석.

**교훈**: `supabase config push` 는 diff 를 보여주지만 **묻지 않고 적용한다.** 부분 설정 파일로 실행하면 안 된다.

## E2E 진행 상태

signUp API 직접 호출 결과 (2026-08-16 08:56):
```
HTTP 200
confirmation_sent_at : 2026-08-16T08:56:05Z   ← 인증 메일 발송됨
email_confirmed_at   : None                    ← 미인증
access_token         : 없음                    ← session null (설계대로)
새 auth user id      : 47420616-a2d6-4ac0-af89-19527b18f529
```

현재 `w2center@naver.com` profiles 행 **2건** (병합 대기 상태):
- `27ee097a…` notes=`예비 멤버`, phone=`''`  ← 옛 예비멤버 행
- `47420616…` notes=null, phone=`01012345678` ← 트리거가 만든 새 행

→ 메일 링크 클릭 후 첫 로그인 시 `claim_provisional_profile()` 이 옛 행을 흡수·삭제하여 **1건**이 되어야 정상.

## ✅ E2E 최종 결과 (2026-08-16, REST 실측)

| 검증 항목 | 결과 |
|---|---|
| 이메일 인증 완료 | `email_confirmed_at = 2026-08-16T09:04:14Z` ✅ |
| 인증 후 로그인 | `grant_type=password` → **200, access_token 발급** ✅ |
| 프로필 완성 루틴 실행 | `user_metadata.waat_profile_pending = false` (true→false 전환됨) ✅ |
| 예비멤버 병합 | `w2center@naver.com` profiles **2건 → 1건**, 옛 행 `27ee097a…` 삭제됨 ✅ |
| 예비멤버 마크 제거 | 남은 행 `notes = null` (`예비 멤버` 없음) ✅ |
| 프로필 데이터 복원 | `name=승승센터`, `phone=01012345678` 정상 반영 ✅ |

**남은 문제 B 해제** — 실제 로그인 세션 토큰으로 `PATCH /profiles?id=eq.47420616…`
(`name`/`phone`/`about_me`/`interests`(배열)/`notes`) 실행 → **200, 1행 반영**.
RLS 가 자기 프로필 UPDATE 를 막지 않음이 확인됐고, `about_me` 컬럼·`interests` 배열도 정상 수용.
(검증용으로 넣은 `about_me` 값은 `''` 로 원복 완료)

## Phase 4 산출물 (전부 검증 완료)

| 문제 | 수정 | 검증 |
|---|---|---|
| interests TEXT[] 에 문자열 전송 → 모든 프로필 저장 실패 | `waatNormalizeProfileUpdates()` 추가, `updateProfile` 양쪽에서 정규화 | 단위 8/8 + 실DB 204 |
| `db.js` 가 `const DB` 재선언 → **SyntaxError 로 파일 전체 미로드** (join.html 예비멤버 조회 통째 미작동) | `var _dbExtensions` + 없는 키만 병합, `supabase` → `_supabase` 별칭 | 2-스크립트 로드 재현 10/10 |
| `about_me` 컬럼 부재 → profile.js 저장 전체 실패 | 마이그레이션으로 컬럼 추가 | PGRST204 → **HTTP 204** |
| 예비멤버 중복행 → 전환 미작동 | `claim_provisional_profile()` RPC(SECURITY DEFINER) + 가입 완성 시 호출 | RPC 생성됨, anon 차단 확인 |

적용된 마이그레이션: `20260816174500_about_me_and_claim_provisional.sql` (db push 완료)

## ⚠️ 배포 순서 주의
배포된 사이트는 아직 **옛 코드**다. Confirm email 을 먼저 켜면 signUp 이 session:null 을 반환해
운영 중인 가입이 즉시 깨진다. **반드시 배포 → 그 다음 설정 ON.**

## 설계 결정

**프로필 데이터를 이메일 왕복 구간에서 어떻게 보존하는가**
→ `signUp(options.data)` = `auth.users.raw_user_meta_data` (서버 저장)
- localStorage 대비 장점: 다른 기기/브라우저에서 인증 링크를 눌러도 데이터 보존
- DB 트리거 마이그레이션 불필요 (클라이언트가 세션의 `user_metadata`에서 읽어 복원)

## DB 실측 결과 (service_role 키로 REST 직접 조회, 2026-08-16)

- `profiles` 실제 컬럼: `id, name, phone, interests, member_type, message, role, created_at, updated_at, email, current_job, notes`
- `profiles.interests` = **TEXT[] (배열)**
- `profiles.email` — **UNIQUE 제약 없음**
- `profiles.id` — **auth.users FK 없음** (임의 UUID 삽입이 201로 성공)
- `w2center@naver.com` → profiles에 1건(id `27ee097a…`, notes `예비 멤버`), **auth.users에는 없음** (auth 총 179명)
- `notes ILIKE '%예비 멤버%'` = **93건**

## ✅ 해결한 근본 버그 — interests 타입 불일치 (치명)

`profiles.interests` 는 TEXT[] 인데 코드는 **문자열**을 보내고 있었다.
```
PATCH {"interests":"AI test"} → HTTP 400  22P02 malformed array literal: "AI test"
PATCH {"interests":""}        → HTTP 400  22P02 malformed array literal: ""
PATCH {"interests":[]}        → HTTP 204  ✅
```
빈 문자열조차 거부되므로 **interests 가 포함된 모든 프로필 저장이 항상 실패**했다.
가입 완성 저장·프로필 수정 전 경로가 해당. 재시도 루프가 5회 실패 후 조용히 넘어가며 이를 은폐.

**수정**: `waatNormalizeProfileUpdates()` 공용 헬퍼 추가 (`supabase-config.js`),
`DB.updateProfile` 양쪽 판본(`supabase-config.js`, `db.js`)에서 저장 직전 문자열→배열 정규화.
- 단위 테스트 8/8 통과 (빈문자열·공백·쉼표구분·후행쉼표·기존배열·null·키부재·원본불변성)
- 실 DB 검증: 정규화 payload → HTTP 204, 값 정상 반영, 원복 완료

## 남은 인접 문제 (PO 판단 필요)

| # | 내용 | 심각도 | 상태 |
|---|---|---|---|
| A | `index.html`이 `js/db.js` 미로드 → `DB.getProfileByEmail()` 부재로 예비멤버 프리체크가 항상 조용히 실패 (`main.js`, catch에서 warn만) | **하** | 확인됨·미수정. 재평가: 프리체크의 유일한 동작이 `console.log` 이고(`main.js:1436`), 실제 병합은 서버측 `claim_provisional_profile()` RPC 가 담당 → **기능 영향 없음**. "✅ 예비멤버 등록 확인" 안내를 살릴지(=db.js 로드 추가) 죽은 코드를 지울지 PO 판단 |
| B | RLS `Users can update own profile (no role/email)` 가 email 변경 금지 — 기존값 NULL이면 UPDATE 거부 가능 | — | **해소** — 실 로그인 세션으로 PATCH 200 확인. profile.js 는 payload 에 email 을 넣지 않으므로 이 경로 자체가 발생 안 함 |
| C | ~~Resend 테스트 발신자~~ | — | **해당없음** — PO 확인: Resend 도메인 인증 완료됨. 로컬 `.env`가 낡은 파일 |
| D | **예비멤버 중복행** — 예비멤버는 auth.users 없는 profiles 행. 가입하면 트리거가 **새 id로 두 번째 행**을 만든다. email UNIQUE 제약이 없어 조용히 중복. 코드는 새 행만 보므로 `예비 멤버` 마크가 붙은 옛 행은 영구 고아 → **예비→정규 전환이 근본적으로 미작동** | 치명 | **해소** — `claim_provisional_profile()` RPC 로 병합, E2E 실측에서 2건→1건 확인 |
| E | `profile.js` 가 존재하지 않는 `about_me` 컬럼에 저장 시도 → `PGRST204` 로 **프로필 저장 전체 실패** | 치명 | **해소** — 마이그레이션으로 컬럼 추가, 실 세션 PATCH 200 확인 |
