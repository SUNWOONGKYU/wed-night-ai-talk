# 잔여 기술부채 정리 (전 세션 미반영분)

PO 지시 (2026-09-02): "수정할 거 있으면 오늘 다 수정해버려"

## Phase

- [x] Phase 1: README 운영 URL 최신화 (`wed-night-ai-talk.vercel.app` → `waat.community`)
- [x] Phase 2: 마이그레이션 파일명에서 실명 제거 (공개 저장소 — 본문은 이미 마스킹됨, 파일명만 남음)
- [x] Phase 3: 마이그레이션 폴더 정리 — 1회성 데이터 조작 48건 문서화 + 명명 규칙
- [x] Phase 4: `get_slot_attendees` 과거 회차 노출 차단 (활성 모임 또는 관리자만)
- [x] Phase 5: 관리자 판별 단일화 — `profiles.role='admin'` 하나로 (지금 3곳 하드코딩)
- [x] Phase 6: CSP `script-src 'unsafe-inline'` 제거 — 인라인 핸들러 17개·인라인 script 4개 정리
- [x] Phase 7: 전체 검증 (로컬 → 배포 → 라이브) + 회귀 스크립트 재실행

## 위험도·주의

- **Phase 5 가 가장 위험** — 틀리면 PO 가 관리자 페이지에 못 들어간다.
  · 먼저 대상 계정에 `role='admin'` 을 넣고, 그게 실제로 들어갔는지 확인한 **뒤에** 판별식을 바꾼다.
  · `is_admin()` 은 SECURITY DEFINER 라 함수 안에서는 컬럼 권한이 적용되지 않는다.
    (정책에 `EXISTS(... profiles.role ...)` 를 직접 인라인하면 깨진다 — 오늘 실측으로 확인했다)
  · 관리자가 0명이 되면 EXCEPTION 으로 롤백한다.
- **Phase 2 한계** — 파일명을 바꿔도 git 히스토리의 옛 파일명은 남는다.
  본문 실명은 히스토리에도 없다(처음부터 마스킹된 채 커밋됨). PO 에게 이 한계를 알렸다.
- **Phase 3 주의** — 이미 적용된 마이그레이션 파일을 폴더 밖으로 옮기지 않는다.
  `supabase db push` 가 참조하므로 물리적 이동은 위험하다. 문서화로 처리한다.
- **Phase 6** — `style-src 'unsafe-inline'` 은 유지한다. `style="..."` 속성이 사이트 전반에 쓰여
  제거하려면 CSS 전면 개편이 필요하고, script-src 만큼 위험하지도 않다.

## 결과 (2026-09-02)

라이브 waat.community 실브라우저 검증
- `verify-csp-and-admin.cjs` 24/24 (실제 CSP 헤더가 걸린 상태)
- `verify-profiles-authenticated-lockdown.cjs` 14/14 (회귀 없음)
- `verify-attendance-lockdown.cjs` 7/7 (회귀 없음)
- 지난 회차 명단 익명 실측: event 3 / 12 / 19 모두 `[]`
- CSP 헤더 실측: `script-src 'self' https://cdn.jsdelivr.net` — unsafe-inline 제거 확인

미검증 (자격증명 필요 — PO 확인 항목)
- 관리자 로그인 → 관리자 페이지 진입 (판별 기준이 이메일 → profiles.role 로 바뀜)
- 관리자 화면 버튼 전부 (인라인 onclick → data-action 위임으로 전환)
- 게시판 관리자 수정·삭제 버튼 (speakup isAdmin() 이 role 기준으로 바뀜)
- 이메일 발송 (Edge Function 권한 검사가 role 조회로 바뀜)

## 이번 작업 중 새로 발견 (미조치, 별도 보고)

`js/speakup-inquiry.js` 가 참조하는 `nav-inquiry-link` 요소가 어느 페이지에도 없다.
speakup.html 푸터의 '문의하기'는 `index.html#footer-inquiry-link` 로 이동하므로,
speakup.html 안의 문의 모달은 **여는 경로가 없는 죽은 마크업**이다.
이번 CSP 작업과 무관한 기존 상태이고, 동작에 해는 없다.
**PO 결정(2026-09-02): 마크업 삭제.** 같은 폼이 두 군데 있으면 나중에 한쪽만 고치는
사고가 나므로 게시판 쪽 죽은 사본을 지웠다. 문의는 index.html 에서만 받는다.
  · speakup.html 모달 마크업 35줄 삭제
  · js/speakup-inquiry.js 삭제 (그 모달 전용 스크립트였다)
  · 푸터의 '문의하기' 링크(index.html 로 이동)는 그대로 — 사용자 동선 변화 없음
  · css 는 공용 클래스(modal-overlay·site-form)라 손대지 않았다
