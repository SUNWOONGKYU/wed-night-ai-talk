# WAAT 예비 회원 시스템 설계안

## 1. 개요

111명의 카톡·페이스북 수집 연락처를 **예비 회원**으로 등록하고, 
정식 회원 전환 프로세스를 구현하는 시스템.

---

## 2. 핵심 요구사항

| 요구사항 | 설명 |
|---------|------|
| **중복 방지** | 이미 가입한 회원의 이메일 검사 후 등록 |
| **일괄 등록** | 111명을 이름 + 이메일 만으로 빠르게 등록 |
| **정보 완성 유도** | 예비 회원 로그인 시 "개인정보를 완성하세요" 알람 |
| **부분 정보 입력** | 전화번호, 소개, 관심사 등 추가 정보 입력 가능 |
| **정식 회원 전환** | 모든 필수 정보 입력 후 "정식 회원" 상태로 변경 |

---

## 3. 기술 구현 방식 비교

### Option A: Supabase Invite 기능 (권장 ⭐)
```
✓ 자동 이메일 발송
✓ 사용자가 자신의 비밀번호 직접 설정
✓ 초대 링크 만료 관리 가능
✗ 대량 초대 시 이메일 봇으로 분류될 수 있음
```

**프로세스:**
1. CSV 업로드 → Supabase Admin API로 초대 생성
2. 각 사용자에게 초대 이메일 발송 (Supabase 자동)
3. 사용자가 링크 클릭 → 이름 + 새 비밀번호 입력
4. 로그인 후 추가 정보 입력

---

### Option B: 수동 계정 생성 + 임시 비밀번호 (대안)
```
✓ 완전 제어 가능
✓ 이메일 발송 안 함 (스팸 우려 없음)
✗ 사용자가 비밀번호를 어디서 받는지 헷갈릴 수 있음
```

**프로세스:**
1. CSV → Supabase 일괄 등록 (임시 비밀번호 자동 생성)
2. 사용자에게 따로 알림 (카톡, 페이스북, 메일)
3. 첫 로그인 시 비밀번호 변경 강제
4. 추가 정보 입력 유도

---

### Option C: 예비 회원 전용 테이블 (경량)
```
✓ 기존 회원과 분리 가능
✓ 개인정보 최소화
✗ 정식 회원 전환 시 데이터 마이그레이션 필요
```

**프로세스:**
1. provisional_members 테이블 생성 (이름, 이메일만)
2. 일괄 등록 (로그인 없이도 가능)
3. 예비 회원이 이메일 링크 클릭 → Supabase 계정 생성
4. 프로필 완성 후 정식 회원으로 전환

---

## 4. 권장 방안: **하이브리드 (Option A + B)**

### 4.1 DB 스키마 확장

**profiles 테이블에 컬럼 추가:**
```sql
ALTER TABLE profiles ADD COLUMN (
    is_provisional BOOLEAN DEFAULT true,        -- 예비 회원 여부
    profile_completion_percent INT DEFAULT 0,   -- 정보 완성도 (%)
    last_completion_prompt_at TIMESTAMP,        -- 마지막 완성 알람 시간
    phone VARCHAR(20),                           -- (이미 있음)
    bio TEXT,                                    -- 자기소개
    interests TEXT,                              -- 관심사 (쉼표 분리)
    preferred_slot TEXT                          -- 선호 시간대
);
```

### 4.2 정보 완성도 계산

```javascript
// JavaScript 예시
function getProfileCompletionPercent(profile) {
    const fields = {
        name: !!profile.name,           // 20%
        phone: !!profile.phone,         // 20%
        bio: !!profile.bio,              // 20%
        interests: !!profile.interests,  // 20%
        // 추가 필드 계산
    };
    return Math.round((Object.values(fields).filter(Boolean).length / 4) * 100);
}
```

### 4.3 예비 회원 등록 프로세스

#### Step 1: 일괄 등록 (관리자가 CSV 업로드)
```sql
-- admin.html의 "일괄 임포트" 기능에서
INSERT INTO auth.users (email, password_hash, confirmed_at)
VALUES 
  ('jbt3101@gmail.com', 'hashed_temp_pwd', now()),
  ('ground_cosmetics@naver.com', 'hashed_temp_pwd', now()),
  ...

INSERT INTO profiles (id, name, email, is_provisional)
VALUES 
  (user_id_1, 'jbt3101', 'jbt3101@gmail.com', true),
  (user_id_2, '유튜브 맨땅에화장품', 'ground_cosmetics@naver.com', true),
  ...
```

#### Step 2: 사용자에게 알림 (카톡/이메일)
```
📢 WAAT 예비 회원 등록 안내

안녕하세요!

당신의 이메일로 WAAT 예비 회원이 등록되었습니다.

🔐 임시 비밀번호: [자동 생성된 비밀번호]
🔗 로그인 링크: https://www.waat.community

첫 로그인 후:
1. 비밀번호 변경
2. 전화번호 추가
3. 자기소개, 관심사 입력

✨ 정보를 완성하면 정식 회원이 됩니다!
```

#### Step 3: 첫 로그인 시 정보 완성 유도

**profile.html 수정:**
```html
<!-- 예비 회원 알람 배너 추가 -->
<div id="provisional-banner" class="banner-warning" style="display:none;">
    ⚠️ <strong>예비 회원</strong>입니다. 
    <a href="#profile-form">개인정보를 완성해주세요</a> 
    (완성도: <span id="completion-percent">0</span>%)
</div>

<!-- 프로필 수정 폼 추가 -->
<form id="profile-form" class="profile-edit-form" style="display:none;">
    <div class="form-group">
        <label>전화번호 *</label>
        <input type="tel" id="edit-phone" placeholder="010-1234-5678">
    </div>
    <div class="form-group">
        <label>자기소개</label>
        <textarea id="edit-bio" placeholder="나를 소개해주세요" rows="3"></textarea>
    </div>
    <div class="form-group">
        <label>관심사</label>
        <input type="text" id="edit-interests" placeholder="AI, 데이터, 창업 등 (쉼표로 구분)">
    </div>
    <button type="submit" class="btn-primary">저장</button>
</form>
```

**profile.js 수정:**
```javascript
async function pfInit() {
    // ... 기존 코드 ...
    
    // 예비 회원 체크
    if (pfCurrentProfile && pfCurrentProfile.is_provisional) {
        document.getElementById('provisional-banner').style.display = 'block';
        document.getElementById('profile-form').style.display = 'block';
        
        // 완성도 표시
        const percent = getProfileCompletionPercent(pfCurrentProfile);
        document.getElementById('completion-percent').textContent = percent;
    }
}

async function pfSaveProfile() {
    const phone = document.getElementById('edit-phone').value;
    const bio = document.getElementById('edit-bio').value;
    const interests = document.getElementById('edit-interests').value;
    
    await DB.updateProfile(pfCurrentUser.id, {
        phone,
        bio,
        interests,
        // 모든 필드 입력되면 is_provisional = false로 변경
        is_provisional: !(phone && bio && interests)
    });
    
    // 갱신 후 재로드
    await pfInit();
}
```

---

## 5. 관리자 페이지 (admin.html) 확장

### 5.1 "예비 회원 일괄 등록" 섹션

```html
<div class="admin-section">
    <h3>📋 예비 회원 일괄 등록</h3>
    
    <div class="form-group">
        <label>CSV 파일 (이름, 이메일)</label>
        <input type="file" id="provisional-csv" accept=".csv,.xlsx">
        <small>형식: 이름, 이메일</small>
    </div>
    
    <button id="btn-import-provisional" class="btn-primary">
        예비 회원 등록
    </button>
    
    <div id="import-result" class="import-result"></div>
</div>

<div class="admin-section">
    <h3>👥 예비 회원 현황</h3>
    <table class="admin-table">
        <thead>
            <tr>
                <th>이메일</th>
                <th>이름</th>
                <th>완성도</th>
                <th>상태</th>
                <th>등록일</th>
                <th>작업</th>
            </tr>
        </thead>
        <tbody id="provisional-list"></tbody>
    </table>
</div>
```

### 5.2 관리자 기능

| 기능 | 설명 |
|------|------|
| **CSV 임포트** | 111명 일괄 등록 |
| **중복 검사** | 기존 회원 이메일 자동 필터링 |
| **알림 재발송** | 예비 회원에게 임시 비밀번호 재발송 |
| **정식 전환** | 관리자가 강제로 정식 회원 전환 |
| **현황 조회** | 예비 회원 수, 완성도 통계 |

---

## 6. 구현 단계

| 단계 | 작업 | 소요 시간 |
|------|------|---------|
| **1단계** | DB 스키마 확장 (마이그레이션 파일 생성) | 30분 |
| **2단계** | admin.html에 CSV 임포트 UI 추가 | 1시간 |
| **3단계** | admin.js에서 Supabase 일괄 계정 생성 로직 | 1시간 |
| **4단계** | profile.html 예비 회원 알람 및 정보 입력 폼 추가 | 1시간 |
| **5단계** | profile.js 완성도 계산 및 저장 로직 | 1시간 |
| **6단계** | 테스트 및 배포 | 1시간 |
| **합계** | | **약 5~6시간** |

---

## 7. 주의사항

### 🔐 보안
- 임시 비밀번호는 **절대 평문으로 저장 금지** (해시 필수)
- 예비 회원용 RLS (Row Level Security) 정책 설정
- 임시 비밀번호 만료 기간 설정 (예: 7일)

### 📧 이메일
- 대량 발송 시 스팸 필터 주의
- SendGrid, Mailgun 등 신뢰도 높은 서비스 사용 권장
- 언서브스크라이브 링크 필수 포함

### 📊 데이터
- 111명 일괄 등록 전에 **백업** 필수
- 중복 이메일 자동 필터링
- 가입 후 로그인 기록 추적 (스팸/봇 방지)

---

## 8. 예상 일정

```
Day 1-2: 설계 + DB 마이그레이션
Day 2-3: 관리자 UI + 일괄 임포트 로직
Day 3-4: 예비 회원 로그인 UX 구현
Day 4-5: 테스트 + 배포 준비
Day 5: Go Live! 🚀
```

---

## 다음 단계

**선택:**
1. ✅ **이 설계로 바로 구현** → 기술 담당자에게 인계
2. 🤔 **상세 검토** → 상황에 따라 조정
3. 💬 **추가 질문** → 특정 부분 심화 설계

